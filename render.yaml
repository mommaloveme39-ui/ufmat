services:
  - type: web
    name: ai-translator-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn bot:app
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: AI_API_KEY
        sync: false
